

export const commonGridOptions = {
    size: 'mini' as const,
    border: 'inner' as const,
    columnConfig: {
        resizable: true,
    },
    rowConfig: {
        resizable: true,
    },
    resizableConfig: {

    },
    customConfig: {
        // storage: {
        //     resizable: true,
        // }
    }
}