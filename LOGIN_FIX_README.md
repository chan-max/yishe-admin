# 登录自动退出问题修复说明

## 问题描述
某些账号在登录后会立刻调用退出登录接口，导致无法正常登录。

## 问题原因
在 `src/store/modules/user.ts` 文件的 `setUserInfoAction` 方法中，当获取用户信息失败时会自动调用 `loginOut()` 方法，这个逻辑过于激进，任何获取用户信息的失败（包括网络问题、服务器临时错误等）都会导致自动退出登录。

## 修复方案

### 1. 优化错误处理逻辑
- **之前**: 任何获取用户信息失败都会自动退出登录
- **现在**: 只有token无效(401错误)时才退出登录，其他错误使用缓存数据

### 2. 添加重试机制
- 最多重试2次获取用户信息
- 每次重试间隔1秒
- 重试失败后使用缓存的用户信息

### 3. 增强调试信息
- 添加详细的控制台日志
- 记录每次尝试的结果
- 显示错误详情和响应状态

## 修复后的行为

### 正常情况
1. 用户登录成功
2. 获取用户信息成功
3. 正常进入系统

### 网络问题
1. 用户登录成功
2. 获取用户信息失败（网络问题）
3. 自动重试2次
4. 重试失败后使用缓存数据
5. 正常进入系统（不会退出登录）

### Token无效
1. 用户登录成功
2. 获取用户信息失败（401错误）
3. 自动退出登录
4. 跳转到登录页面

## 代码变更

### 主要修改文件
- `src/store/modules/user.ts` - 优化 `setUserInfoAction` 方法

### 关键改进
```typescript
// 之前：任何错误都退出登录
try {
  userInfo = await getInfo()
} catch (e) {
  await this.loginOut() // 过于激进
  return
}

// 现在：智能错误处理
while (retryCount < maxRetries) {
  try {
    userInfo = await getInfo()
    break
  } catch (e) {
    if (e?.response?.status === 401) {
      // 只有token无效才退出
      await this.loginOut()
      return
    }
    // 其他错误使用缓存数据
    if (retryCount >= maxRetries) {
      userInfo = wsCache.get(CACHE_KEY.USER)
      if (!userInfo) {
        await this.loginOut()
        return
      }
    }
  }
}
```

## 测试建议

### 1. 正常登录测试
- 使用有效账号登录
- 确认能正常进入系统

### 2. 网络问题测试
- 模拟网络不稳定情况
- 确认不会自动退出登录

### 3. Token失效测试
- 使用过期token
- 确认会正确退出登录

## 监控和调试

### 控制台日志
修复后会显示详细的控制台日志：
- 🔄 开始获取用户信息...
- 📡 尝试获取用户信息 (第 1 次)
- ✅ 成功获取用户信息
- ❌ 获取用户信息失败
- ⚠️ 多次尝试失败，尝试使用缓存的用户信息
- 🚫 Token无效，退出登录

### 错误信息
- 响应状态码
- 响应数据内容
- 重试次数和结果

## 注意事项

1. **缓存依赖**: 修复依赖用户信息缓存，确保缓存机制正常工作
2. **重试次数**: 当前设置为最多重试2次，可根据需要调整
3. **日志级别**: 生产环境可能需要调整日志级别
4. **兼容性**: 修复保持向后兼容，不影响现有功能

## 后续优化建议

1. **配置化**: 将重试次数和间隔时间配置化
2. **监控**: 添加登录成功率监控
3. **用户体验**: 在网络问题时显示友好提示
4. **缓存策略**: 优化用户信息缓存策略

---

**修复完成时间**: 2025-01-27  
**修复人员**: AI Assistant  
**测试状态**: 待测试
